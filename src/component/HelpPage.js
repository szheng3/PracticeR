import React from 'react';
import PropTypes from 'prop-types';

const HelpPage = props => {
    return (
        <div>

            this is help page
        </div>
    );
};

HelpPage.propTypes = {


};

export default HelpPage;
